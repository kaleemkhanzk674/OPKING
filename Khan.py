import sys, platform, os
os.system('git pull >/dev/null')
bit = platform.architecture()[0]
if bit == '64bit':
    try:
        import TC
    except KeyboardInterrupt:
        exit()
elif bit == '32bit':
    try:
        import TC
    except KeyboardInterrupt:
        sys.exit(https://github.com/kaleemkhanzk674/kaleemkhanzk674)
    except:
        sys.exit("92189794317?=tks")